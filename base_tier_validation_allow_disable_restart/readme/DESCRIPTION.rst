Allow to disable the restart validation feature in tier definitions.

Disable can be done by tier definition. It mean if you have multiple sequences 
on a validation tier, you can disable the restart validation feature for a specific sequence.
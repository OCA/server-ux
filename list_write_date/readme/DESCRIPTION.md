`write_date` field allow to search last updated records.

This module add `write_date` field in all list views (hidden by default).

Then each list views can becomes a `last activity view`.

To keep this module lightweight, the One2many list does not include this feature.
